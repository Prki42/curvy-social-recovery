# Curvy social key recovery

## Testing

- Run all tests: `go test ./key_recovery/...`

## Benchmarking

- Run benchmarks and tests: `go test -bench=. ./key_recovery/...`
- Run only benchmarks: `go test -bench=. ./key_recovery/... -run=^$`